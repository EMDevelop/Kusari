version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pipenv install
        - pipenv shell
    build:
      commands:
        - npm run client
        - npm run server
  backend:
    phases:
      postBuild:
        commands:
          - python manage.py migrate
  artifacts:
    baseDirectory: frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
