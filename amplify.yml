version: 1
frontend:
  phases:
    preBuild:
      commands:
        - sudo -H pip3 install -U pipenv
        - pipenv shell
        - cd frontend
        - npm install
        - cd ..
    build:
      commands:
        - npm run client
        - npm run server
  backend:
    phases:
      postBuild:
        commands:
          - python manage.py migrate
  artifacts:
    baseDirectory: frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
